<h2>新規登録</h2>

<%= form_with model: @user, url: user_registration_path do |f| %>

  <p>※保護施設で登録した場合、施設名（カナ含）・電話番号・住所は公開される情報になります。</p>
  <div class="field">
    <%= f.radio_button :category, "private_user", checked: "checked" %>
    <%= f.label :category, "個人ユーザー" %>
    <br>
    <%= f.radio_button :category, "shelter" %>
    <%= f.label :category, "保護施設" %>
  </div>

  <div class="field">
    <%= f.label :お名前or施設名称 %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :お名前or施設名称（カナ） %><br>
    <%= f.text_field :name_kana %>
  </div>

  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :電話番号（ハイフンなし） %><br>
    <%= f.text_field :telephone_number %>
  </div>

  <div class="field">
    <%= f.label :住所 %><br>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :パスワード %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> 文字以上)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :パスワード（確認用） %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>
  
  <div class="field">
    <%= f.label :ユーザー画像 %><br>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit "新規登録",data: { confirm: '※『保護施設』で登録する場合は、施設名称・電話番号・住所・サイト登録日が公開されます。よろしいですか？' } %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
